type GalleryItem {
  id: String!
  title: String!
  description: String
  content: String
  imagePath: String
  remoteObject: [String!]
  keyDetailId: ID
  createdAt: Date
  updatedAt: Date
}

input GalleryItemInput {
  title: String!
  description: String
  content: String
  imagePath: String
  remoteObject: [String!]
  keyDetailId: ID
}

type GalleryQueries {
  list: [GalleryItem]
  locate(id: ID!): GalleryItem
  listByRemoteObjects(ids: [String!]!): [GalleryItem]
}

type GalleryMutations {
  create(input: GalleryItemInput): QueryResult
}

extend type Query {
  galleryQueries: GalleryQueries!
}

extend type Mutation {
  galleryMutations: GalleryMutations!
}

type StandardError {
  message: String!
}

type QuerySuccess {
  message: String!
  id: ID
}

union QueryResult = QuerySuccess | StandardError
