type Song {
  id: String!
  name: String
  stems: [Stem]
}

type Stem {
  filePath: String!
  name: String
}

type StemPlayerQueries {
  list: [Song]
  locate(id: ID!): Song
}

extend type Query {
  stemPlayerQueries: StemPlayerQueries!
}
