mutation updateBlogPost($id: ID!, $input: BlogPostInput!) {
  blogMutations {
    updateBlogPost(id: $id, input: $input) {
      __typename
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
