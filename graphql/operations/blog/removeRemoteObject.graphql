mutation removeRemoteObject($postId: ID!, $target: String!) {
  blogMutations {
    removeRemoteObject(postId: $postId, target: $target) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
