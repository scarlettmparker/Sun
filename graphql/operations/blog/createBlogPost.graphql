mutation createBlogPost($title: String!, $input: BlogPostInput!) {
  blogMutations {
    createBlogPost(title: $title, input: $input) {
      ... on QuerySuccess {
        message
        id
      }
      ... on StandardError {
        message
      }
    }
  }
}
