mutation saveRegistry($input: HubRegistryInput!) {
  gaiaMutations {
    saveRegistry(input: $input) {
      message
      registry {
        mode
        apps {
          key
          name
          dir
          devPort
          prodPort
          url
          description
          enabled
          self
        }
      }
    }
  }
}
